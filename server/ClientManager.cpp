#include "ClientManager.h"
#include <iostream>

// TODO: Move client-related functions from server/main.cpp into this file.
// Functions to move include: InteractWithClient, SendToClient, authenticatedUsers map and related helpers.

void InteractWithClient(SOCKET clientSocket) {
    // Placeholder wrapper so build doesn't break while refactoring.
    std::cout << "ClientManager::InteractWithClient scaffold for socket " << clientSocket << std::endl;
}
